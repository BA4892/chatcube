export interface ChatPageRouteParams {
  sessionId?: string
  sessionTitle?: string
  modelId?: string
  providerId?: string
  initialPrompt?: string
  initialDraft?: string
  initialToolIds?: string
}
